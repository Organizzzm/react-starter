const initialState = [{
    text: 'Use Redux!!',
    marked: false,
    id: 0
}];

export function todos(state = initialState, action) {
    switch (action.type) {
        case 'asdfasf':
            return state;

        default:
            return state;
    }
}